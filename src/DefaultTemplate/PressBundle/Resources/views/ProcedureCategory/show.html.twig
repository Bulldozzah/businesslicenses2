{% extends '::base.html.twig' %}

{% block body -%}
    <h1>ProcedureCategory</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ entity.title }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('procedurecategory') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
