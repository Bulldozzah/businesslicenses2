{% trans_default_domain 'FOSUserBundle' %}
<div class="content">
	<!-- BEGIN FORGOT PASSWORD RESET FORM -->
    <form class="login-form" action="{{ path('admin_user_reset_password', {'token': token}) }}" {{ form_enctype(form) }} method="POST" class="fos_user_resetting_reset">

	    <div style="color: white" class="form-group">
		{{ form_widget(form) }}
        </div>

		<div class="form-actions">
			<button type="submit" class="btn btn-warning uppercase pull-right">Reset Password</button>
		</div>
	</form>
	<!-- END FORGOT PASSWORD FORM -->
	
</div>
