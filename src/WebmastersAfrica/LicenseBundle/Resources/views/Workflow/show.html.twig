{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Workflow</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ entity.title }}</td>
            </tr>
            <tr>
                <th>Sendemails</th>
                <td>{{ entity.sendEmails }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('license_workflow') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('license_workflow_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
