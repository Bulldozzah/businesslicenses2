[www]
; Pass Railway's environment variables (MYSQL_URL, APP_SECRET, ...) through to PHP.
clear_env = no
catch_workers_output = yes
pm = dynamic
pm.max_children = 10
pm.start_servers = 3
pm.min_spare_servers = 2
pm.max_spare_servers = 5
